name: "CodeQL - Android"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '38 10 * * 5'

jobs:
  analyze:
    name: Analyze (Java/Android)
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'java' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'gradle'

    # Initialize CodeQL tools for scanning.
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        build-mode: manual
        # If you wish to specify custom queries, you can do so here or in a config file.
        # queries: security-extended,security-and-quality

    # Setup Node.js for Capacitor sync (required before Android build)
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    # Sync Capacitor to generate capacitor-cordova-android-plugins folder
    # This folder is gitignored, so it must be generated in CI before build
    - name: Install frontend dependencies and sync Capacitor
      run: |
        cd frontend
        npm ci
        npx cap sync android

    # MANUAL BUILD STEP
    # The default autobuild often fails for Android or mixed projects.
    # We explicitly build the debug variant to resolve dependencies.
    - name: Build Android App (Manual)
      run: |
        cd frontend/android
        chmod +x gradlew
        ./gradlew assembleDebug --no-daemon

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"
