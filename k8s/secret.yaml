# =============================================================================
# KUBERNETES SECRETS TEMPLATE
# =============================================================================
# IMPORTANT: This file is a TEMPLATE. Do NOT commit actual secrets to Git!
#
# Usage:
# 1. Copy this file to your local machine (do not commit the copy)
# 2. Replace the placeholder values with base64-encoded real secrets:
#    echo -n "your-actual-secret" | base64
# 3. Apply directly to cluster: kubectl apply -f secret.yaml
#
# For production, use:
# - Sealed Secrets: https://sealed-secrets.netlify.app/
# - External Secrets Operator: https://external-secrets.io/
# - HashiCorp Vault: https://www.vaultproject.io/
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: banking-secret
  namespace: banking-app
type: Opaque
data:
  # Replace with: echo -n "your-strong-secret-key" | base64
  SECRET_KEY: REPLACE_WITH_BASE64_ENCODED_SECRET_KEY
  # Replace with: echo -n "postgresql://user:pass@host:5432/db" | base64
  DATABASE_URL: REPLACE_WITH_BASE64_ENCODED_DATABASE_URL
  # Replace with: echo -n "your-db-password" | base64
  DATABASE_PASSWORD: REPLACE_WITH_BASE64_ENCODED_DB_PASSWORD
  # Replace with: echo -n "admin:strongpassword" | base64
  FLOWER_BASIC_AUTH: REPLACE_WITH_BASE64_ENCODED_FLOWER_AUTH
  # Replace with: echo -n "your-redis-password" | base64 (or leave empty)
  REDIS_PASSWORD: ""
