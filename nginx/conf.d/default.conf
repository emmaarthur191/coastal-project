server {
    listen 80;
    listen [::]:80;
    server_name yourdomain.com;

    # Security Headers
    include /etc/nginx/security-headers.conf;

    # Frontend React App
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;

        # Cache static assets
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # API Proxy with security controls
    location /api/ {
        proxy_pass http://django_backend;

        # Security controls
        limit_req zone=apilimit burst=20 nodelay;
        limit_except GET POST PUT DELETE { deny all; }

        # Request size limits
        client_max_body_size 10M;

        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;

        # Timeouts
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # Static files
    location /static/ {
        alias /app/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Media files
    location /media/ {
        alias /app/media/;
        expires 6M;
        add_header Cache-Control "public";
        # Consider adding authentication for sensitive media
    }

    # Health checks (internal only)
    location /health/ {
        internal;
        proxy_pass http://django_backend/health/;
        access_log off;
    }
}

# HTTPS server block - ENABLE WHEN SSL CERTIFICATES ARE AVAILABLE
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name yourdomain.com;
#
#     # SSL Configuration (MUST in production)
#     ssl_certificate /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#
#     # Security Headers
#     include /etc/nginx/security-headers.conf;
#
#     # Frontend React App
#     location / {
#         root /usr/share/nginx/html;
#         index index.html index.htm;
#         try_files $uri $uri/ /index.html;
#
#         # Cache static assets
#         location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
#             expires 1y;
#             add_header Cache-Control "public, immutable";
#         }
#     }
#
#     # API Proxy with security controls
#     location /api/ {
#         proxy_pass http://django_backend;
#
#         # Security controls
#         limit_req zone=apilimit burst=20 nodelay;
#         limit_except GET POST PUT DELETE { deny all; }
#
#         # Request size limits
#         client_max_body_size 10M;
#
#         # Headers
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-Host $host;
#
#         # Timeouts
#         proxy_connect_timeout 30s;
#         proxy_send_timeout 30s;
#         proxy_read_timeout 30s;
#     }
#
#     # Static files
#     location /static/ {
#         alias /app/static/;
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
#
#     # Media files
#     location /media/ {
#         alias /app/media/;
#         expires 6M;
#         add_header Cache-Control "public";
#         # Consider adding authentication for sensitive media
#     }
#
#     # Health checks (internal only)
#     location /health/ {
#         internal;
#         proxy_pass http://django_backend/health/;
#         access_log off;
#     }
# }

# HTTP to HTTPS redirect
server {
    listen 80;
    listen [::]:80;
    server_name yourdomain.com;
    return 301 https://$server_name$request_uri;
}